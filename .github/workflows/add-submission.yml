name: Add submission

on:
  issues:
    types: [opened, edited]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  open-pr:
    if: contains(github.event.issue.labels.*.name, 'submission')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse issue and open PR
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.issue.body || "";

            // Issue forms render each field as "### Label\n\nvalue"
            function field(label) {
              const re = new RegExp(`### ${label}\\s*\\n\\s*([^\\n]*(?:\\n(?!### )[^\\n]*)*)`, "i");
              const match = body.match(re);
              if (!match) return "";
              const value = match[1].trim();
              return value === "_No response_" ? "" : value;
            }

            const name = field("App name");
            const repoUrl = field("Repository URL");
            const downloadUrl = field("Download / Releases URL");
            const author = field("Your GitHub username");
            const category = field("Category");
            const description = field("Description");
            const screenshots = field("Screenshots")
              .split("\n")
              .map((s) => s.trim())
              .filter(Boolean);
            const lightSdk = field("Built with the Light SDK?") === "Yes";

            const missing = [];
            if (!name) missing.push("App name");
            if (!repoUrl) missing.push("Repository URL");
            if (!author) missing.push("Your GitHub username");
            if (!category) missing.push("Category");
            if (!description) missing.push("Description");
            if (screenshots.length === 0) missing.push("Screenshots");

            if (missing.length) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `I couldn't parse this submission — missing or unreadable field(s): ${missing.join(", ")}. Please edit the issue using the submission form fields and I'll retry automatically.`,
              });
              return;
            }

            const slug = name
              .toLowerCase()
              .trim()
              .replace(/[^a-z0-9]+/g, "-")
              .replace(/^-+|-+$/g, "");
            const dirName = `${author}_${slug}`;
            const entryPath = `content/apps/${dirName}/index.md`;

            const { data: existing } = await github.rest.repos.getContent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              path: "content/apps",
              ref: "main",
            }).catch(() => ({ data: [] }));

            if (Array.isArray(existing) && existing.some((e) => e.name === dirName)) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `It looks like **${name}** by **${author}** is already on the list (\`${dirName}\`). If this is a mistake, let us know!`,
              });
              return;
            }

            const yamlStr = (s) => JSON.stringify(s);
            const dateAdded = new Date().toISOString().slice(0, 10);

            const frontmatterLines = [
              "---",
              `title: ${yamlStr(name)}`,
              `date_added: ${dateAdded}`,
              `repo: ${yamlStr(repoUrl)}`,
              `download: ${yamlStr(downloadUrl)}`,
              `author: ${yamlStr(author)}`,
              `author_url: ${yamlStr(`https://github.com/${author}`)}`,
              `category: ${yamlStr(category)}`,
            ];
            if (lightSdk) frontmatterLines.push("light_sdk: true");
            frontmatterLines.push(
              "images:",
              ...screenshots.map((url) => `  - ${yamlStr(url)}`),
              `description: ${yamlStr(description)}`,
              "---",
              "",
            );

            const content = frontmatterLines.join("\n");

            const branch = `submission/issue-${context.issue.number}`;
            const { data: mainRef } = await github.rest.git.getRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: "heads/main",
            });

            await github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: `refs/heads/${branch}`,
              sha: mainRef.object.sha,
            }).catch(() => {}); // branch may already exist on re-edit

            const existingFile = await github.rest.repos.getContent({
              owner: context.repo.owner,
              repo: context.repo.repo,
              path: entryPath,
              ref: branch,
            }).catch(() => null);

            await github.rest.repos.createOrUpdateFileContents({
              owner: context.repo.owner,
              repo: context.repo.repo,
              path: entryPath,
              message: `Add ${name} to the list`,
              content: Buffer.from(content).toString("base64"),
              sha: existingFile ? existingFile.data.sha : undefined,
              branch,
            });

            const existingPrs = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              head: `${context.repo.owner}:${branch}`,
              state: "open",
            });

            if (existingPrs.data.length === 0) {
              const { data: pr } = await github.rest.pulls.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: `Add ${name}`,
                head: branch,
                base: "main",
                body: `Adds **${name}** to the list.\n\nCloses #${context.issue.number}`,
              });

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `Thanks for the submission! I opened #${pr.number} — a maintainer will review it shortly.`,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: `Updated #${existingPrs.data[0].number} with your latest edits.`,
              });
            }
